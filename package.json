{
  "name": "light-and-dark-mode-website",
  "version": "1.0.0",
  "description": "A simple website with light and dark mode toggle functionality",
  "main": "index.html",
  "scripts": {
    "start": "http-server .",
    "build": "mkdir -p dist && cp index.html dist/ && npx uglify-js script.js -o dist/script.min.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "light-mode",
    "dark-mode",
    "website",
    "theme-toggle"
  ],
  "author": "Your Name",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^14.1.1",
    "uglify-js": "^3.17.4"
  }
}
